# 20260707 Benchmark 9ch feature-analysis task note

## Current role

This task is an exploratory signal-feature view for the Tsinghua Benchmark 9ch
setting. It complements decoder benchmarks by producing PSD, harmonic SNR,
time-frequency, simple topographic, and feature-vs-accuracy artifacts.

## Current formal output

Keep this result directory as the usable output:

- `results/feature_probe`
  - Small-subject / selected-target feature probe.
  - Includes `report.md`, feature CSVs, FBCCA selected-target predictions, and
    figures.

## Legacy outputs

The old duplicated smoke output is archived under:

- `results/_legacy/20260707_smoke_runs/`

It is retained for provenance only and should not be used as the current report
input.

## Next cleanup

- If this line becomes report-critical, promote the selected figures from
  `feature_probe/figures` into the corresponding report or analysis task.
- Keep expanded all-subject probes under explicit result names rather than
  reusing `smoke`.
