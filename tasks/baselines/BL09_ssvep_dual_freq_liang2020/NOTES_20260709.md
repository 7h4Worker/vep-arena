# NOTES 2026-07-09

## Scope

Started a task-owned smoke workflow for the Liang2020 dual-frequency/phase dataset. This is separate from the already completed Dual-Alpha and Binocular AR tasks.

## Dataset Checks

- Local root: `D:/ProjData/datasets/ssvep_dual_frequency_phase_liang2020`.
- Downloaded readme says the sampling rate is 1000 Hz.
- Channel order is `Pz, PO5, PO3, POz, PO4, PO6, O1, Oz, O2`.
- Triggers `1-12` or `1-40` correspond to target indexes.
- Extracted data currently contains 708 `.mat` files across `exp1`-`exp4`.
- Files mix MATLAB formats: some load through `scipy.io.loadmat`, others require HDF5 via `h5py`.
- Canonical tensor orientation for this task is `(channels, samples, trials)`.

## Current Decision

The first runner only builds inventory/trial tables and one time/frequency QA figure. Classification is intentionally deferred until the experiment-condition frequency/phase mapping is pinned to the paper.

The downloaded readme gives sampling rate, channel list, and trigger range but does not include a machine-readable frequency/phase codebook. Do not treat `successTrialTypeId` as a frequency map; it is only the target index.

## Smoke Result

Command:

```powershell
.\.venv\Scripts\python.exe tasks\ssvep_dual_frequency_phase_liang2020\run.py --subjects 1 --experiments exp1,exp2 --max-files-per-experiment 4
```

Output root: `tasks/ssvep_dual_frequency_phase_liang2020/results/smoke_inventory`.

Observed:

- 8 `.mat` files loaded.
- 464 trial rows exported.
- `exp1` subject 1 condition 1: 4 files, 144 trials, labels 1-6, 2.5 s windows.
- `exp2` subject 1 condition 1: 4 files, 320 trials, labels 1-40, 2.0 s windows.
- QA figure written to `liang2020_smoke_time_frequency.png`.
