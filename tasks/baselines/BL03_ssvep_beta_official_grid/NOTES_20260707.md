# 20260707 BETA official-grid task note

## Current role

This task is a compact BETA analysis/plot view for the toolbox documentation
window grid: `0.25`, `0.5`, `0.75`, and `1.0` seconds.

It should stay separate from `beta_ssvep_9ch_baselines`, which covers the
broader 0.2-2.0 s baseline line.

## Current output

Keep current task outputs under:

- `results/`

## Legacy status

No result directories were moved on 2026-07-07 because this task currently has
a single compact result root and no clear superseded smoke output.

## Next cleanup

- If the official-grid plot is reused in a report, add a manifest that records
  the source summaries and preprocessing version.
