# 20260707 BETA 9ch baselines task note

## Current role

This task owns the BETA 9-channel baseline comparison line for CCA, FBCCA, TRCA,
ETRCA, and TDCA under subject-specific leave-one-block-out evaluation.

## Current output

Keep current task outputs under:

- `results/`

The README notes that some historical source summaries may still live under the
repository-level `results/` directory and are used only as a plotting fallback.
Those files should be migrated into task-owned outputs before this line becomes
part of a final report bundle.

## Legacy status

No result directories were moved on 2026-07-07 because the task currently has a
single result root and no obvious superseded smoke directory in the task itself.

## Next cleanup

- If rerunning BETA, create explicit result names for the protocol and window
  grid, then archive older summaries under `results/_legacy/`.
- Avoid mixing official-grid comparison outputs with the broader 0.2-2.0 s
  baseline line.
