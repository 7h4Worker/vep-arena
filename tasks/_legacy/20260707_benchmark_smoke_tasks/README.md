# 20260707 Benchmark Smoke Tasks

These task folders were moved out of the active `tasks/` root on 2026-07-07:

- `benchmark_dnn_smoke`
- `benchmark_ssvepformer_smoke`
- `benchmark_trcanet_smoke`

They are retained for provenance and quick runnable examples only. Current
Benchmark method comparisons should use the main Benchmark task outputs and
shared runner scripts instead of treating these smoke folders as active task
roots.
