[project]
name = "vep-arena"
version = "0.1.0"
description = "Clean VEP/SSVEP benchmark arena"
requires-python = ">=3.11,<3.13"
dependencies = [
  "numpy",
  "scipy",
  "scikit-learn",
  "pandas",
  "matplotlib",
]

[tool.uv]
package = false
